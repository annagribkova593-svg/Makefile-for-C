# Имя исполняемого файла
TARGET = hello

# Компилятор
CC = gcc

# Флаги компиляции (например, для отладки или оптимизации)
CFLAGS = -Wall -Wextra -std=c11

# Исходный файл
SRC = main.c

# Объектный файл (опционально, но полезно знать)
OBJ = main.o

# Основная цель: сборка программы
$(TARGET): $(SRC)
	$(CC) $(CFLAGS) -o $@ $<

# Команда для очистки скомпилированных файлов
clean:
	rm -f $(TARGET) $(OBJ)

# Команда для запуска программы
run: $(TARGET)
	./$(TARGET)

# Фиктивные цели (phony targets), чтобы избежать конфликтов с файлами с такими именами
.PHONY: clean run